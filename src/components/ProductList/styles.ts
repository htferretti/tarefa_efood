import styled from "styled-components"

export const List = styled.section`
    padding: 80px 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-gap: 80px;
    width: 1024px;
    margin: 0 auto;
`